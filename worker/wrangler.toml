name = "vapi-agent"
main = "src/index.js"
compatibility_date = "2024-01-01"

[vars]
SUPABASE_URL = "https://rffvoreqcpkqqxwprosg.supabase.co"

# Secrets (set via `wrangler secret put`):
# SUPABASE_ANON_KEY   — Supabase API key for DB operations
# VAPI_SECRET         — Shared secret for Vapi webhook authentication
# SLACK_WEBHOOK_URL   — Slack incoming webhook for notify_manager alerts

[triggers]
routes = []
