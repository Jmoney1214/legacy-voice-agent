# OpenAI — copy to openai_config.env (gitignored) and fill in
# Same account as the Vapi voice stack (Riley runs on gpt-4o-mini)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Planner Agent model — cheap and sufficient for task-spec generation
OPENAI_MODEL=gpt-4o-mini
